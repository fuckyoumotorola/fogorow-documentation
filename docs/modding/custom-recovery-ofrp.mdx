---
title: Custom recovery (OFRP)
---
import ofrpImg from "@site/static/assets/custom-recovery/ofrp.png";

export const images = [
    {
        "android_version": "14",
        "url": "https://github.com/fuckyoumotorola/recovery_device_motorola_fogorow-OFRP/releases/download/31892865833/vendor_boot.img"
    },
    {
        "android_version": "15",
        "url": "https://github.com/fuckyoumotorola/recovery_device_motorola_fogorow-OFRP/releases/download/27692558425/vendor_boot.img"
    },
    {
        "android_version": "16",
        "url": "https://github.com/fuckyoumotorola/recovery_device_motorola_fogorow-OFRP/releases/download/31852639622/vendor_boot.img"
    }
];

:::warning
Make sure you have the [bootloader unlocked](../dev/bootloader.md) or you will get soft brick!
:::

## OFRP

<img src={ofrpImg} width="200" />

### Bugs

- Impossible to flash some logical partitions while on [FastbootD](../modes/fastboot.mdx#fastbootd) mode

### Install

export const grouped = images.reduce((acc, item) => {
    if (!acc[item.android_version]) {
        acc[item.android_version] = [];
    }
    acc[item.android_version].push(item);
    return acc;
}, {});

{Object.keys(grouped).map(payload_version => (
    <details key={payload_version} className="custom-details">
        <summary>
            OrangeFox Android {payload_version}
        </summary>
        <ul>
            {grouped[payload_version].map(item => (
                <li key={item.android_version}>
                    <a href={item.url}>
                        {item.android_version}
                    </a>
                </li>
            ))}
        </ul>
    </details>
))}

<br />

1. Download release for your specific android version
2. Reboot to [fastboot mode](../modes/fastboot)
3. Disable vbmeta (if partition not found - ignore)
    ```shell
    $ fastboot flash vbmeta vbmeta.img --disable-verity --disable-verification
    ```
4. Flash OFRP
    ```shell
    $ fastboot flash vendor_boot vendor_boot.img
    ```

### Credits
- Device tree: [Tiziano Tomas Luzi Ramos](https://github.com/tizianoluziramos)
- Original TWRP device tree: [Ryszard Kryjak](https://github.com/korzenroot)