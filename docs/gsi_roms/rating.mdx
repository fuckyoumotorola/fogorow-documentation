---
title: Rating
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Link from "@docusaurus/Link";

import { a14, a15, a16 } from "./gsi_roms.js";

<Tabs>
    <TabItem value="a14" label="Android 14" default>
        <Tabs>
            <TabItem value="platinum" label="Platinum" default>
                {a14.platinum.map((item, index) => (
                    <Link key={index} target="_blank" to={item.link} style={{ display: 'block', marginBottom: '8px' }}>
                        {item.name}
                    </Link>
                ))}
            </TabItem>
            <TabItem value="gold" label="Gold">
                {a14.gold.map((item, index) => (
                    <Link key={index} target="_blank" to={item.link} style={{ display: 'block', marginBottom: '8px' }}>
                        {item.name}
                    </Link>
                ))}
            </TabItem>
            <TabItem value="silver" label="Silver">
                {a14.silver.map((item, index) => (
                    <Link key={index} target="_blank" to={item.link} style={{ display: 'block', marginBottom: '8px' }}>
                        {item.name}
                    </Link>
                ))}
            </TabItem>
            <TabItem value="bronze" label="Bronze">
                {a14.bronze.map((item, index) => (
                    <Link key={index} target="_blank" to={item.link} style={{ display: 'block', marginBottom: '8px' }}>
                        {item.name}
                    </Link>
                ))}
            </TabItem>
            <TabItem value="broken" label="Broken">
                {a14.broken.map((item, index) => (
                    <Link key={index} target="_blank" to={item.link} style={{ display: 'block', marginBottom: '8px' }}>
                        {item.name}
                    </Link>
                ))}
            </TabItem>
        </Tabs>
    </TabItem>

    <TabItem value="a15" label="Android 15">
        <Tabs>
            <TabItem value="platinum" label="Platinum" default>
                {a15.platinum.map((item, index) => (
                    <Link key={index} target="_blank" to={item.link} style={{ display: 'block', marginBottom: '8px' }}>
                        {item.name}
                    </Link>
                ))}
            </TabItem>
            <TabItem value="gold" label="Gold">
                {a15.gold.map((item, index) => (
                    <Link key={index} target="_blank" to={item.link} style={{ display: 'block', marginBottom: '8px' }}>
                        {item.name}
                    </Link>
                ))}
            </TabItem>
            <TabItem value="silver" label="Silver">
                {a15.silver.map((item, index) => (
                    <Link key={index} target="_blank" to={item.link} style={{ display: 'block', marginBottom: '8px' }}>
                        {item.name}
                    </Link>
                ))}
            </TabItem>
            <TabItem value="bronze" label="Bronze">
                {a15.bronze.map((item, index) => (
                    <Link key={index} target="_blank" to={item.link} style={{ display: 'block', marginBottom: '8px' }}>
                        {item.name}
                    </Link>
                ))}
            </TabItem>
            <TabItem value="broken" label="Broken">
                {a15.broken.map((item, index) => (
                    <Link key={index} target="_blank" to={item.link} style={{ display: 'block', marginBottom: '8px' }}>
                        {item.name}
                    </Link>
                ))}
            </TabItem>
        </Tabs>
    </TabItem>

    <TabItem value="a16" label="Android 16">
        <Tabs>
            <TabItem value="platinum" label="Platinum" default>
                {a16.platinum.map((item, index) => (
                    <Link key={index} target="_blank" to={item.link} style={{ display: 'block', marginBottom: '8px' }}>
                        {item.name}
                    </Link>
                ))}
            </TabItem>
            <TabItem value="gold" label="Gold">
                {a16.gold.map((item, index) => (
                    <Link key={index} target="_blank" to={item.link} style={{ display: 'block', marginBottom: '8px' }}>
                        {item.name}
                    </Link>
                ))}
            </TabItem>
            <TabItem value="silver" label="Silver">
                {a16.silver.map((item, index) => (
                    <Link key={index} target="_blank" to={item.link} style={{ display: 'block', marginBottom: '8px' }}>
                        {item.name}
                    </Link>
                ))}
            </TabItem>
            <TabItem value="bronze" label="Bronze">
                {a16.bronze.map((item, index) => (
                    <Link key={index} target="_blank" to={item.link} style={{ display: 'block', marginBottom: '8px' }}>
                        {item.name}
                    </Link>
                ))}
            </TabItem>
            <TabItem value="broken" label="Broken">
                {a16.broken.map((item, index) => (
                    <Link key={index} target="_blank" to={item.link} style={{ display: 'block', marginBottom: '8px' }}>
                        {item.name}
                    </Link>
                ))}
            </TabItem>
        </Tabs>
    </TabItem>
</Tabs>